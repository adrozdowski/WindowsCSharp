<UserControl x:Class="BundledFun.HighScores"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">
    
    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}">
        <StackPanel>
            <TextBlock Text="High scores:" FontSize="{StaticResource ResourceKey=PhoneFontSizeLarge}" Margin="24" />
            <TextBlock Name="wyn1" Margin="12" HorizontalAlignment="Center" />
            <TextBlock Name="wyn2" Margin="12" HorizontalAlignment="Center" />
            <TextBlock Name="wyn3" Margin="12" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</UserControl>